function checkValuesIsEqual(firstValue: string, secondValue: string) : boolean {
  if (firstValue === secondValue) {
    return true;
  } else {
    return false
  }
}

export default checkValuesIsEqual;